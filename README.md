# practice2
## Requirements: Registration Form

1. Form: name, email, password, role selection (radio), consent (checkbox)
2. Table: class schedule (3+ rows)
3. Each field has a `<label>`
4. Submit button (currently without JS — `type="submit"`)

**Criteria:**
- [ ] Tab navigation works for all fields
- [ ] The `input` has `type` and `name` specified
- [ ] The table has a `<thead>` with headings
